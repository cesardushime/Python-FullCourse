import math
first = 'John'
last = 'Smith'
#a formated string is the one starting with an "f"

msg = f'{first} [{last}] is a coder'
print(msg)
#string methods, such as len(), etc. methods such as course.upper() (they are referring to something else) course.upper() course.lower() course.title() course.find() course.replace()
#Module has different sections of codes that can be re-usable
#You can use the math function using the . calling
#we use elif as short for if else

